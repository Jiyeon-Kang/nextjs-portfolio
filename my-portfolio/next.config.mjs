/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'media.gcflearnfree.org',
            'upload.wikimedia.org'
        ],
    },
};

export default nextConfig;
